default: &default
  adapter: redis
  url: <%= "redis://redis:#{ENV.fetch('REDIS_PORT')}/1" %>
  password: <%= ENV.fetch('REDIS_PASSWORD') %>

development:
  <<: *default
  channel_prefix: murfin_method_development

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch('REDIS_URL') %>
  channel_prefix: murfin_method_production
  # network_timeout: 5
