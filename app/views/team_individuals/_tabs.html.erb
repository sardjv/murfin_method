<div class='thin-tabs'>
  <%=
    render(
      partial: 'shared/tabs',
      locals: {
        tabs: [
          {
            label: I18n.t('nav.tabs.summary', team_name: @user_group.name), path: team_individual_path(@user_group, @user)
          },
          {
            label: I18n.t('nav.tabs.data'), path: data_team_individual_path(@user_group, @user)
          }
        ]
      }
    )
  %>
</div>